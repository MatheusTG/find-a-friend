{
  "$schema": "https://turbo.build/schema.json",
  "envMode": "strict",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "start:dev": {
      "persistent": true,
      "cache": false
    },
    "format": {},
    "format:check": {}
  }
}
